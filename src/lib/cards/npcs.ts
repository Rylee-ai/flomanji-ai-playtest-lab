
import { NPCCard } from "@/types/cards/npc";
import { log } from "@/utils/logging";

// Export empty NPC cards collection
export const NPC_CARDS: NPCCard[] = [];

log.info("Placeholder NPC cards initialized");
