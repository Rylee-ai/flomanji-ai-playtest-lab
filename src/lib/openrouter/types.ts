
export interface OpenRouterMessage {
  role: 'system' | 'user' | 'assistant';
  content: string;
}

export interface OpenRouterResponse {
  choices: Array<{
    message: {
      content: string;
    };
  }>;
}

// Adding more comprehensive types for OpenRouter models
export interface OpenRouterModel {
  id: string;
  name: string;
  description?: string;
  context_length?: number;
  pricing?: {
    prompt?: number;
    completion?: number;
  };
}

// Interface for the response from the OpenRouter models endpoint
export interface OpenRouterModelsResponse {
  data: OpenRouterModel[];
}
