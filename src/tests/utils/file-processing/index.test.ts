
import { describe, test } from 'vitest';

// This is a main test file that imports all test modules
// We can add integration tests here if needed

describe('FileProcessingService', () => {
  test('all tests are in respective modules', () => {
    // This test just ensures the test runner loads this file
    // All actual tests are in the imported modules
  });
});
